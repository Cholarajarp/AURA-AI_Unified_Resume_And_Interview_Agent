import React from 'react';
import ReactDOM from 'react-dom/client';
import AuraAgent from './AuraAgent.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AuraAgent />
  </React.StrictMode>,
);
